#!/usr/bin/env node
const path = require('path')
const { run, flush, handle } = require('@oclif/core')

// packages/server/bin/run -> root should be packages/server
const root = path.resolve(__dirname, '..')

run(process.argv.slice(2), { root })
  .then(flush)
  .catch(handle)